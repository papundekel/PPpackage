name: Test

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Try run.sh
      run: |
        ./run.sh tmp/ <tmp/input/input-basic-noconan.json
